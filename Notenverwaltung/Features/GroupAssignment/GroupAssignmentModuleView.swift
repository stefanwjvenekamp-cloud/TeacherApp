import SwiftUI

struct GroupAssignmentModuleView: View {
    var body: some View {
        FeatureTemplateView(
            title: "Gruppeneinteilung",
            icon: "person.3",
            iconColor: .Theme.teal,
            subtitle: "Gruppen aus Klassenlisten erstellen",
            description: "Modulstruktur vorbereitet. Verwendet später direkt die gemeinsamen Schüler- und Klassendaten."
        )
    }
}
